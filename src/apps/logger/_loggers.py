import logging

externalServiceLogger = logging.getLogger("external_service_logger")

celeryLogger = logging.getLogger("celery_logger")

errorRequestLogger = logging.getLogger("error_request_logger")

kafkaLogger = logging.getLogger("kafka_logger")
